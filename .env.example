# Application Environment
NODE_ENV=development
PORT=5000
DEBUG=delivery-website:*

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/delivery-website

# Production Deployment
# Uncomment and set these for production deployments
# NODE_ENV=production
# MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/delivery-website?retryWrites=true&w=majority

# Optional: API Rate Limiting
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100

# Optional: CORS Configuration
# CORS_ORIGIN=https://yourdomain.com

# Optional: Authentication (if using JWT)
# JWT_SECRET=your-secret-key-here
# JWT_EXPIRATION=24h

# Platform-Specific (Heroku)
# HEROKU_APP_NAME=delivery-website

# Platform-Specific (Vercel)
# VERCEL_ENV=production
# VERCEL_URL=delivery-website.vercel.app

# Platform-Specific (AWS)
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=your-bucket-name
